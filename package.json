{
  "name": "iphone-ssh-mcp",
  "version": "1.0.0",
  "description": "Standalone MCP server for jailbroken iPhone over SSH",
  "main": "dist/server.js",
  "scripts": {
    "test": "node --test --import tsx test/**/*.test.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "check": "npm run build && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bin": {
    "iphone-ssh-mcp": "./dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
